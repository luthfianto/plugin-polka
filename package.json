{
  "name": "merapi-plugin-polka",
  "version": "0.0.1",
  "description": "Merapi Plugin for Polka App",
  "main": "index.js",
  "scripts": {
    "test": "./node_modules/.bin/mocha",
    "cover": "./node_modules/.bin/istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -R spec && cat ./coverage/lcov.info | ./node_modules/.bin/codacy-coverage && rm -rf ./coverage"
  },
  "keywords": [
    "polka",
    "merapi"
  ],
  "dependencies": {
    "body-parser": "*",
    "merapi": "*",
    "polka": "^*",
    "server-address": "^2.0.0",
    "type-check": "*"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "chai-http": "^3.0.0",
    "codacy-coverage": "^2.0.3",
    "istanbul": "^0.4.5",
    "mocha": "^3.2.0",
    "mocha-lcov-reporter": "^1.3.0",
    "supertest": "^3.0.0"
  }
}
